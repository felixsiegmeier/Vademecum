[tool.vulture]
paths = ["."]
ignore_names = ["cls"]
min_confidence = 80
exclude = ["tests/", ".venv/"]

[tool.ruff]
exclude = ["tests/", ".venv/"]
target-version = "py314"

[tool.ruff.lint]
select = ["F"]
