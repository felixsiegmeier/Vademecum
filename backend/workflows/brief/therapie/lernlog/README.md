# lernlog — brief/therapie

Gelernte Formatierungsregeln für die Therapie-Sektion.

`default.yml` ist gitignored und entsteht beim ersten Lerntrigger.
Format: YAML mit `schema_version` + `rules`-Liste (siehe example.yml).
