# lernlog — brief/verlauf

Gelernte Stilregeln für die Verlauf-Sektion (Compound-Level).
Gilt für alle drei Passes (collect, audit, curate) via Orchestrator.

`default.yml` ist gitignored und entsteht beim ersten Lerntrigger.
Format: YAML mit `schema_version` + `rules`-Liste (siehe example.yml).
